import React from 'react'

function CartSummaryTitle({title}: {title: string}) {
  return (
    <h2 className="text-xl font-semibold text-gray-900 dark:text-white mb-4">{title}</h2>

  )
}

export default CartSummaryTitle
